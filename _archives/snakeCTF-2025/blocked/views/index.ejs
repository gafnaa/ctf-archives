<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Captcha Verification</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body class="flex flex-col items-center">
    <h1 class="text-3xl font-bold mt-8">Checking if the site connection is secure...</h1>

    <div class="flex flex-col items-center gap-4 bg-black/10 border border-black/5 p-4 rounded-lg mt-4">
      <div class="captcha-container">
        <div class="captcha-background-wrapper" id="bg-wrapper">
          <img src="" id="captcha-bg" alt="Captcha Background" />
        </div>
        <img
          src=""
          id="captcha-fg"
          class="captcha-foreground"
          alt="Captcha Foreground"
        />
      </div>

      <input
        type="range"
        id="slider"
        min="0"
        max="400"
        value="0"
        class="w-full"
      />

      <div class="flex gap-4 w-full">
        <input
          type="text"
          class="border border-gray-300 w-full rounded px-4 py-2"
          id="solution-input"
          placeholder="Enter the solution"
        />
        <button id="submit-btn" class="px-8 py-2 bg-black rounded text-white">Submit</button>
      </div>
    </div>

    <div class="mt-2 text-lg font-semibold text-center">
      You need to solve 10 captchas in 10 seconds.
      <br />
      You solved <span id="remaining-captchas">0</span> captchas.
    </div>

    <div id="result-message"></div>

    <script src="/script.js"></script>
  </body>
</html>